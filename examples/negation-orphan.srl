# SPARQL 1.2 RL: stratified negation as failure (NOT { ... }).
PREFIX : <http://example/>

DATA {
  :alice a :Person .
  :bob a :Person .
  :alice :childOf :carol .
}

RULE { ?x a :Orphan } WHERE { ?x a :Person . NOT { ?x :childOf ?y } }
