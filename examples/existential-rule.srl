# Companion to existential-rule.n3/.eye: the same rule-head blank node,
# using SRL's BNODE() to mint a fresh witness per human.

PREFIX : <http://example.org/socrates#>

DATA { :Socrates a :Human. :Plato a :Human. }

RULE { ?s :is ?w } WHERE { ?s a :Human. SET(?w := BNODE()) }
